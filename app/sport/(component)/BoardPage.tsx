import React from "react";
import BoardContainer from "@/app/components//boards/BoardContainer";

const BoardPage = () => {
  return <BoardContainer typ={2} page={1} size={15} />;
};

export default BoardPage;
